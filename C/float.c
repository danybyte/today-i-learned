#include <stdio.h>

int main(){
    // for float number:
    float score = 3.5;
    float price = 19.99;
    // and you have to write %f for float var
    printf("your score is %f\n", score);
    printf("the price is $%.2F", price);
    return 0;
}
